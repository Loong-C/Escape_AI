# C++ Optimized Engine 验证记录

验证日期：2026-09-04（Asia/Shanghai）

## 构建与静态检查

- MSVC 19.44、C++20、Python 3.12、pybind11 3.1.0：构建通过。
- CTest 原生测试：通过。
- Ruff、Mypy strict：通过。
- Pytest：51 项测试通过。

## Python/C++ 差分验证

- 固定种子：`20260904`。
- 尺寸：3、5、9、17。
- 每尺寸 20 盘，共 80 盘、5453 个状态、5373 手。
- 每个状态比较完整桩位、球、行动方、结果、合法动作及放置/替换类型、四向第一步代价、四边界距离、最短首步集合、全部墙和全部锚桩。
- 同时比较 C++ 二进制状态往返、Zobrist 风格稳定哈希、D4 状态变换和动作变换。
- 结果：零差异。

## 初始性能基准

`scripts/benchmark_engine.py --size 17 --games 5 --seed 20260904` 使用相同种子和完全相同的 626 手随机动作轨迹分别运行两套引擎：Python 为 3.947 秒，C++ 为 0.0165 秒，观察到约 240 倍加速。该数字只用于建立本机回归基线，不代表后续 MCTS 的端到端加速比。
